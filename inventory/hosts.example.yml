all:
  vars:
    # Define credenciales/conexion por ambiente
    # ansible_user: automation
    # ansible_ssh_private_key_file: ~/.ssh/id_rsa
    ansible_python_interpreter: auto_silent
  children:
    migration_targets:
      hosts:
        host1.example.com:
        host2.example.com:
