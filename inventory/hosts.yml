all:
  vars:
    # Inventario base intencionalmente vacio para evitar ejecuciones accidentales.
    # Usa inventory/hosts.example.yml como referencia y agrega tus hosts reales aqui.
    ansible_python_interpreter: auto_silent
  children:
    migration_targets:
      hosts: {}
